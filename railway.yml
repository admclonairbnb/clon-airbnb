
env: 
    MONGO_DB_NAME: ${{ secrets.MONGO_DB_NAME}}
    MONGO_DB_URI: ${{ secrets.MONGO_DB_URI}}
    SECRET: ${{ secrets.SECRET}}

services:
    app:
      commands:
        - npm install
        - npm start
      port: 3000
      routes:
        - http://airbnbClon:3000